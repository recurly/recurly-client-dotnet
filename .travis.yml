language: csharp
# dist: trusty
mono: none
dotnet: 5.0
# solution: Recurly.sln
install:
  # - dotnet restore Recurly.sln
  # - dpkg --purge packages-microsoft-prod && dpkg -i packages-microsoft-prod.deb
  - sudo apt-get update && sudo apt-get upgrade
  - sudo apt-get install -y libssl-dev mono-devel
script:
  - dotnet restore Recurly.sln
  - dotnet build /p:Configuration=Release Recurly.sln
  - ./scripts/test --configuration Release
